# Security Policy

## 安全边界

本程序是本机单用户交易工具，默认只监听 `127.0.0.1`。仪表盘使用单账户登录和 HttpOnly 会话 Cookie，默认账户为 `jaychougo@gmail.com`，不提供注册或多用户权限。非回环部署必须通过 HTTPS 访问；如需公网远程访问，仍应由有经验的运维人员增加访问控制、防火墙和审计。

程序没有提现、转账或修改交易所 API key 的界面/API，但交易签名凭据仍可授权下单、撤单和持仓操作。密钥失窃可能造成资金损失。

## 仪表盘登录

- 使用 `npm run auth:hash` 交互式生成 scrypt 密码哈希，只把输出的 `AUTH_PASSWORD_HASH` 配置到 `.env` 或部署平台；不要填写或提交明文密码；
- 默认唯一账户邮箱为 `jaychougo@gmail.com`，可用 `AUTH_EMAIL` 改成另一个唯一账户，但程序不支持注册和第二个账户；
- `HOST` 为非回环地址时自动要求 HTTPS；反向代理终止 TLS 时，只有在代理可信且链路受控的情况下才设置 `AUTH_TRUST_PROXY=1`；
- 会话 Cookie 使用 `HttpOnly`、`SameSite=Strict`，登录密码不会写入 URL、日志或状态文件。

## 密钥规则

- 永远不要提交 `.env`、`secrets/`、私钥、助记词、代理密码、Webhook token 或 `.state.json`；
- Arcus 和 RHC 优先使用独立私钥文件，并限制该文件仅当前 Windows 用户可读；
- 使用专用、可撤销、最小权限 key；平台支持时配置 IP 白名单；
- 不要使用主钱包私钥；
- 怀疑泄露时先在服务商撤销，再调查原因；
- 秘密一旦进入 Git 历史，仅删除最新文件不够，必须轮换并清理所有历史/附件/缓存。

## 报告漏洞

请不要在公开 Issue 中发布可利用细节、真实账户信息或密钥。优先使用仓库的 **Security → Report a vulnerability** 私下提交；若仓库尚未启用私密报告，请联系维护者建立安全沟通渠道后再发送细节。

报告应包含受影响版本、复现条件、最小化步骤、影响和建议修复，但必须使用模拟账户和假凭据。维护者确认收到前，不要公开零日交易漏洞。

## 第三方风险

交易所、SDK、行情、AI、代理、npm 和 Python 包均属于第三方依赖，其可用性、条款、地域限制和安全状态可能变化。发布者不能保证交易所 API 持续兼容、订单一定成交、断网自动撤单或 AI 输出正确。任何实盘操作都需要交易所官网二次核对和人工应急方案。
